CREATE TABLE `PATH_CLIENT` (
	`CLIENT_KEY` INT PRIMARY KEY AUTO_INCREMENT, 
	`IDENTIFICATION` VARCHAR(20), 
	`ID_TYPE` INT, 
	`LAST_NAME` VARCHAR(100), 
	`FIRST_NAME` VARCHAR(100), 
	`MIDDLE_NAME` VARCHAR(100), 
	`DATE_OF_BIRTH` DATE, 
	`GENDER_KEY` INT, 
	`ETHNICITY_KEY` INT DEFAULT 0, 
	`MARITAL_STATUS_KEY` INT, 
	`AMBULATORY_KEY` INT, 
	`CLIENT_NOTES` TEXT, 
	`CREATE_DATE` DATE, 
	`CREATE_USER_KEY` INT, 
	`HOMELESS` CHAR(1), 
	`REGION_KEY` INT, 
	`SUFFIX` VARCHAR(10), 
	`ANONYMOUS` CHAR(1) DEFAULT 'N', 
	`UPDATE_DATE` DATE, 
	`UPDATE_USER_KEY` INT, 
	`DISABLING_CONDITION` INT DEFAULT 8, 
	`PREVIOUS_FIRST_NAME` VARCHAR(50) DEFAULT NULL, 
	`PREVIOUS_MIDDLE_NAME` VARCHAR(50), 
	`PREVIOUS_LAST_NAME` VARCHAR(50), 
	`PREVIOUS_SUFFIX` VARCHAR(50), 
	`HOURS_WORKED_LAST_WEEK` INT DEFAULT 0, 
	`LOOKING_FOR_WORK` INT DEFAULT 0, 
	`VETERAN` INT DEFAULT 8, 
	`HEALTH_STATUS_KEY` INT, 
	`PREGNANCY_STATUS` INT DEFAULT 0, 
	`PREGNANCY_DUE_DATE` DATE, 
	`TRANSGENDER_TYPE` INT, 
	`PRIMARY_LANGUAGE_KEY` INT, 
	`SECONDARY_LANGUAGE_KEY` INT, 
	`INTERPRETER_NEEDED` CHAR(1) DEFAULT NULL, 
	`PRIMARY_LANGUAGE` INT, 
	`SECONDARY_LANGUAGE` INT, 
	`PICTURE` BLOB, 
	`PICTURE_NAME` VARCHAR(75), 
	`PICTURE_UP_USER` INT, 
	`PICTURE_UP_DT` DATE, 
	`PICTURE_DEL_USER` INT, 
	`PICTURE_DEL_DT` DATE, 
	`PICTURE_TYPE` VARCHAR(5), 
	`RACE_KEY` INT, 
	`HOMELESS_STATUS_KEY` INT, 
	`DOB_TYPE` INT, 
	`INCOME_QUESTION` INT, 
	`INCOME_DATE` DATE, 
	`NON_CASH_QUESTION` INT, 
	`NON_CASH_DATE` DATE, 
	`IS_EMPLOYED` INT, 
	`TENURE` INT, 
	`VOLUNTEER` CHAR(1), 
	`IS_BASIC` CHAR(1) DEFAULT 'N', 
	`UPDATE_TIMESTAMP` TIMESTAMP (6), 
	`MIGRATION_FLAG` CHAR(2), 
	`IS_COORD` CHAR(1), 
	`NAME_TYPE` INT, 
	`GENDER_DESC` VARCHAR(60), 
	`HEALTH_INS_QUESTION` INT, 
	`HEALTH_INS_DATE` DATE, 
	`OTHER_IDENTIFICATION` VARCHAR(30), 
	`OTHER_IDENTIFICATION_NUM` VARCHAR(20)
) ;