CREATE TABLE `HIV_AIDS_STATUS` (
	hivAidsStatusId INT AUTO_INCREMENT PRIMARY KEY,
	enrollmentId INT,
	informationDate DATE,
	response INT,
	indefiniteAndImpairs INT,
	documentationOnFile INT,
	receivingServices INT
);